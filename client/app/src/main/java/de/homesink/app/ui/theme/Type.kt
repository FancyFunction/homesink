package de.homesink.app.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 default type scale. The requirements pin colours, not fonts, so the
 * skeleton ships the platform scale unmodified; a brand typeface would be added
 * here without touching call sites.
 */
internal val HomesinkTypography: Typography = Typography()
