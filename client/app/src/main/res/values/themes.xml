<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Window/system-bar theme only. All in-app colour comes from the Compose
      HomesinkTheme; this exists so the launch window matches before Compose
      draws. Dark variant in values-night/themes.xml.
    -->
    <style name="Theme.Homesink" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
